version: 2

models:
  - name: stg_orders
    description: Orders from the raw seed, typed.
    columns:
      - name: order_id
        tests: [unique, not_null]
      - name: status
        tests:
          - accepted_values:
              values: ['complete', 'cancelled']
